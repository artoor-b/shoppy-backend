version: 0.2
phases:
  install:
    runtime-version:
      nodejs: 20
    commands:
      - node -v
      - npm install
  build:
    commands:
      - npm prisma generate
      - npm build
artifacts:
  files:
    - '**/*'
